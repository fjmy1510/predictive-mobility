# タスクリスト: 予測配置デモの実装

## 完了条件

`docs/` のMust（AC-01〜AC-09）を満たす静的な1画面アプリを実装する。Shouldの予約需要・需要スコア・ログ・予測理由・移動演出も実装し、状態の不変条件とブラウザー動作を検証する。起動手順、実装した構成、検証結果を文書へ反映する。

## 根拠と判断

- 要件原文を変更せず、機能設計の5状態と3操作を採用する。
- HTML・CSS・通常のJavaScriptで、file://起動とオフライン動作を可能にする。実行時の外部依存・保存・API・実時計・位置情報取得なし。
- 状態遷移をDOMから分離した `demo-state.js` を設け、Node.js標準テストランナーで境界値と未割り当て条件を検証する。依存パッケージはアプリに追加しない。
- PREDICTEDを300ms表示。車両の移動は1.6秒。完了イベント・時間による補完・動きを減らす設定に対応する。
- 需要合計1.82は予測配置時点のスナップショットとして保持する。

## 作業

- [x] 要求・機能設計・構成・開発ガイドライン・既存記録を確認する。
- [x] 固定データ、状態処理、単一画面、車両演出を実装する。
- [x] 状態遷移・境界・重複イベントの自動テストを実行する。
- [x] ブラウザーで3操作、再読込、通信非依存、動きを減らす設定、画面サイズとキーボードを確認する。
- [x] 独立した実装レビューを行い、必要な修正を統合する。
- [x] README・構成文書と検証結果を更新する。

## 対象外

将来機能、実データ接続、予約/即時配車フォーム、公開・デプロイ。

## 検証結果

- Node.js v24.13.0で標準テスト5件合格。需要合計の浮動小数点誤差を検出・修正済み。
- Chrome 152.0.7977.83、Edge 153.0.4234.32でfile://起動・オフライン完走・連打防止・再読込・キーボード・reduced-motion・終了イベントなしのfallbackを確認。
- UI更新開始は次の描画機会まで最大13.6ms。車両移動は約1.60秒。
- 幅1440・1366・768・390・320pxで検証。目視で見つかったスマートフォン幅のカード崩れを修正し、表示領域テストを追加して両ブラウザーで再検証。
- 外部HTTP(S)通信0、コンソールエラー0。独立レビューの指摘なし。
- ローカルプレビューはPythonで127.0.0.1:8765、HTTP 200を確認。
- 詳細と証拠へのリンクは `docs/implementation-validation.md` に記録。

## 残る問題・再開情報

実装上の未完了項目なし。審査員役による30秒の理解確認（NFR-04）、会場端末・実スマートフォン・Safari/Firefoxの検証は未実施。会場環境の用意後、3操作を通して確認する。

## 完了時の記録

2026-09-13、MustとShould全項目を実装。起動に必要な5ファイル、状態・ブラウザーテスト2ファイル、文書・検証記録を整備。公開・デプロイ・コミットは実施していない。
