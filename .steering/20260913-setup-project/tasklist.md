# タスクリスト: プロジェクト文書の初期整備

## 完了条件

差し替え版の「予測配置型オンデマンド交通システム 要件定義書 ― 1時間ハッカソンMVP版」を基準に、要求・機能設計・アーキテクチャ・リポジトリ構造・開発ガイドライン・用語集を整備する。将来要件とMVP、確定要件と設計上の補完を区別し、要求と検証を対応させる。

## 根拠と判断

- 2026-09-13、作業開始時は `.git/` のみ。実装、設定、CI、既存文書、コミットはない。
- ユーザーの setup-project 呼び出しが作業範囲。今回は文書整備であり、アプリ実装・依存導入・公開は含まない。
- 最初のチャット内要件は差し替え済み。以後は添付 `pasted-text.txt` の45章からなる新版を唯一の要件基準とする。
- 新版のデモ状態は5つ。旧版の4状態設計は使用しない。
- 原文を `docs/product-requirements.md` に保存し、仕様の補完と未決定事項は設計文書に記録する。
- HTML・CSS・JavaScript の静的構成を設計案とする。実装済み・ユーザー選定済みとは扱わない。

## 作業

- [x] スキル、作業フォルダー、差し替え資料を確認する。
- [x] 差し替え版の原文を要求定義書として保存する。
- [x] 5状態、3操作、未割り当ての予測配置を機能設計にする。
- [x] MVPと将来構想を分けた構成・配置・開発運用・用語を記述する。
- [x] 文書への入口を作成する。
- [x] 原文一致、リンク先の実在、FR・NFR・ACの対応、状態と優先度の整合を検証する。

## 対象外・置換

- 初版の4状態を前提とする設計は新版の5状態に置換する。
- アプリ実装・インストール・動作試験・デプロイは今回の文書整備の対象外。

## 検証結果

- `Get-FileHash -Algorithm SHA256` で、差し替え添付と `docs/product-requirements.md` が完全一致することを確認。
- PowerShellでローカルMarkdownリンク22件を抽出し、すべての参照先が実在することを確認。
- 原文のFR 11件・NFR 5件・AC 9件について、設計・検証手順の参照を確認。
- 機能設計に5状態を個別定義し、82%で未割り当て、98%で需要確定、明示操作でASSIGNEDになることを文書間で照合。
- 第33・34章に従い、位置変化はMust、アニメーション・予約需要・集計・ログ・予測理由はShouldとして整合を確認。
- 作成した案内・設計文書のコードフェンスと行末空白を確認。要求原文の書式は変更していない。
- `git status --short --untracked-files=all` で追加対象が8ファイルのみであることを確認。グローバルignoreファイルの読み取り権限警告は残るが、一覧取得は成功。途中の `core.excludesFile=NUL` 指定はGitが受け付けず、通常のstatusで確認した。Git設定は変更していない。
- アプリ・ブラウザーの動作試験は未実施。今回は未実装のため、AC・NFRの実動作合格とは扱わない。

## 残る問題・再開情報

文書整備の未完了項目はなし。実装方式の最終選択、会場のブラウザー・表示環境、実装するShouldの範囲は後続作業で決める。PREDICTEDの約300ms表示、需要スコアの集計時点、ボタン制御、補助データ項目は設計上の補完案。完成したアプリの検証は開発ガイドラインに従い、実装後に実施する。

## 完了時の記録

2026-09-13完了。要件原文1文書、関連設計5文書、README、作業記録の計8ファイルを整備。差し替え前の内容を新版に混ぜず、要件原文と実装の補完案を分離した。
